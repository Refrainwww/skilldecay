# Environment template for API experiments.
# Copy to `.env.local.ps1`, fill values locally, and do not commit it.

$env:OPENAI_BASE_URL = "https://your-openai-compatible-endpoint"
$env:OPENAI_API_KEY = "your-openai-compatible-key"
$env:OPENAI_MODEL = "gpt-4o-mini"

$env:DEEPSEEK_API_KEY = "your-deepseek-key"
$env:DEEPSEEK_MODEL = "deepseek-chat"
